import type { Metadata } from "next";
import Variant3Pro from "../_v3pro";

export const metadata: Metadata = { title: "Transparent AI — polished", robots: { index: false, follow: false } };

// Dev preview of the polished radial reasoning-core (Variant 3, refined).
export default function TransparentPro() {
  return <Variant3Pro />;
}
